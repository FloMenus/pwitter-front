import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: "bold",
    alignSelf: "center",
    marginBottom: 15,
  },
  postBtn: {
    marginTop: 10,
  },
});

export default styles;
